service: myTestService
provider:
  name: aws
  runtime: go1.x
  region: eu-west-1
  environment:
    ALLOWED_ORIGIN: ${opt:allowedOrigin}
  iam:
    role:
      statements:
        - Effect: "Allow"
          Action:
            - "dynamodb:PutItem"
            - "dynamodb:Query"
          Resource:
            Fn::Join:
              - ":"
              - - "arn:aws:dynamodb"
                - Ref: "AWS::Region"
                - Ref: "AWS::AccountId"
                - table/ecommerce-${opt:stage}
package:
  exclude:
    - ./**
  include:
    - ./bin/**
functions:
  hello:
    handler: bin/hello
  api:
    handler: bin/api
    events:
      - http:
          path: /products
          method: get
      - http:
          path: /categories
          method: get
      - http:
          path: /admin/products
          method: post
      - http:
          path: /admin/categories
          method: post